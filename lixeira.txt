main.c
// Abaixo deve ser comentado "carregarHoraMinutoCorrente()"
// Manipulação direta dos minutos
      if ( IsKeyDown(KEY_A) )
      {
        pa->minuto++;
      }
      if ( IsKeyDown( KEY_D ) )
      {
        pa->minuto--;
      }

      if ( pa->minuto < 0 ) pa->minuto = 60;
      else if ( pa->minuto > 60 ) pa->minuto = 0;

      char num[64];
      sprintf( num, "%i,%f", pa->minuto, pa->anguloGraus );
      DrawText( num, pa->centro->x, pa->centro->y, 20, RED );

// Manipulação direta das horas
      if ( IsKeyDown(KEY_A) )
      {
        pa->hora++;
      }
      if ( IsKeyDown( KEY_D ) )
      {
        pa->hora--;
      }

      if ( pa->hora < 0 ) pa->hora = 24;
      else if ( pa->hora > 24 ) pa->hora = 0;

      char num[64];
      sprintf( num, "%i,%f", pa->hora, pa->anguloGraus );
      DrawText( num, pa->centro->x, pa->centro->y, 20, RED );
